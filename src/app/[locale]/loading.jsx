import Loader from '../[locale]/katalog/lod'

const Loading = () => {
	return (
		<div className=' absolute z-50 w-full h-screen flex justify-center items-center '>
			<Loader />
		</div>
	)
}

export default Loading
